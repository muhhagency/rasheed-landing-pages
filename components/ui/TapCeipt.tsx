// The TapCeipt wordmark. Always set in Michroma via the `.tapceipt` class
// (defined in tokens.css). Never type the string "TapCeipt" inline anywhere
// else — always render this component so the styling can't be missed.
export function TapCeipt() {
  return <span className="tapceipt">TapCeipt</span>;
}
